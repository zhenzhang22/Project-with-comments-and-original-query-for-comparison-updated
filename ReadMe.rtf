The folder Server contains the backend, and the folder client has the front end\
2. Go to the server folder, npm install, and then npm start. Our backend is at localhost8080.\
3. Go to the client folder, npm install, and then npm start. Our frontend is at localhost3000.\

4. The first query at the store info page is intentionally to be slow for query optimization comparison.}
